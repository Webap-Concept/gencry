// components/modules/posts/icons/types.ts
//
// Tipi condivisi dai componenti icona reactions.
export type ReactionIconProps = {
  /** Lato del quadrato (px). Default 24. */
  size?: number;
  /** Classi Tailwind aggiuntive. Tipicamente per drop-shadow o overflow
   *  control nei wrapper accavallati. */
  className?: string;
};
